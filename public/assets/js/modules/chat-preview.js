/* ============================================================
   Chat preview
   Reservado para una futura vista previa del chat en la landing.
   Por ahora no hay elementos [data-chat-shell] en el HTML,
   así que este módulo no hace nada.
   ============================================================ */

export function initChatPreview() {
  const shell = document.querySelector('[data-chat-shell]');
  if (!shell) return;
  // TODO: implementar cuando exista la sección de chat en la landing.
}